//this is the forcast for today
const kelvin = 0;
//converting kelvin to celsius
const celsius = kelvin - 273;
//convert fahrenheit to celsius and make sure the the result is an iteger suing .floor
const fahrenheit = Math.floor(celsius * (9/5) + 32);
//console log and use string interpolation
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit`)

//change the temprature in line 2 to get that result in fahrenheit

// const newton = Math.floor(celsius * (33/100));
// console.log(newton)